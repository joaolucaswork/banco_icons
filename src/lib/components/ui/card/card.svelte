<script>
  import { cn } from "$lib/utils.js";

  let {
    ref = $bindable(null),
    class: className,
    children,
    ...restProps
  } = $props();
</script>

<div
  bind:this={ref}
  data-slot="card"
  class={cn(
    "flex flex-col gap-6 rounded-xl bg-card py-0 text-card-foreground shadow-sm",
    className,
  )}
  {...restProps}
>
  {@render children?.()}
</div>
